#include <stdio.h>

int main(){
  float num[], lower=0, greater=0, i=0;
  printf("Enter a positive numbers: (to stop the reading enter a nugative number)\n");
  while(i>=0){
    printf("--> ");scanf("%f",&num[i]);printf("\n");
    if(num[i]<0){
      i=-1;
    }else{
      if(
    }
  }
     
}
