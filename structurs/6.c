#include <stdio.h>

void main(void){
    struct reference{
        char address1[50];
        char address2[50];
        char city[30];
        char state[30];
        char postal_code[11];
    };


    typedef struct regist{
    };
}